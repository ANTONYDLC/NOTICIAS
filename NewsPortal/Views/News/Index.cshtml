@model IEnumerable<NewsPortal.Models.Post>

<h1>Noticias</h1>

<ul>
@foreach(var post in Model)
{
    <li>
       <a href="@Url.Action("Details", "News", new { id = post.Id })">@post.Title</a>

    </li>
}
</ul>
